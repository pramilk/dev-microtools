/**
 * Slugs whose primary function depends on an on-device AI model (see each tool's own
 * content page for which model, and its License and attribution section for credit) —
 * used only to show a small "AI" indicator on the homepage tool cards, purely a display
 * concern like `toolIcons.ts`, not anything a tool's own logic reads.
 */
export const AI_POWERED_TOOL_SLUGS = new Set(['background-remover', 'face-plate-blur']);
